@{

}
@model ZoneTech.Models.DepartamentoML
@{
    
}
<a class="btn btn-success" asp-controller="Departamento" asp-action="DepartamentoNuevo">Agregar</a>

<h1>Lista de Departamentos</h1>
<table class="table table-dark table-striped" id="tablelist">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Nombre</th>
      <th>
        <th scope="col">Opción</th>
      </th>
    </tr>
  </thead>
  <tbody>
    @foreach (var item in ViewBag.listado)
    {
      <tr>
        <td>@item.DepartamentoId</td>
        <td>@item.Nombre</td>
         <td>
          <td>
            @Html.ActionLink("Eliminar","DepartamentoEliminar",new{id=@item.DepartamentoId},new{@class="btn btn-danger"})
            @Html.ActionLink("Editar","DepartamentoEditar",new{id=@item.DepartamentoId},new{@class="btn btn-warning"})
          </td>
        </td>
      </tr>
    }
  </tbody>
</table>

@section scripts{
    <script src="https://cdn.datatables.net/1.11.3/js/jquery.dataTables.min.js"></script>

    <script>
      $(document).ready(function(){
        //alert("Read load");
        var tabla = $('#tablelist').DataTable();
      });
    </script>
}
