@{

}
<h1>Lista de Articulos</h1>
<table class="table table-dark table-striped" id="tablelist">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Nombre</th>
      <th scope="col">Precio</th>
      <th scope="col">Cantidad</th>
      <th scope="col">SubTotal</th>
      <th>
        <th scope="col">Opciones</th>
      </th>
    </tr>
  </thead>
  <tbody>
    @foreach (var item in ViewBag.list)
    {
      <tr>
        <td>@item.ArticuloId</td>
        <td>@item.Nombre</td>
        <td>@item.Precio</td>
        <td>@item.Cantidad</td>
        <td>@item.SubTotal</td>
        <td>
          <td>
            @Html.ActionLink("Admin","DeleteArticulo",new{id=@item.ArticuloId},new{@class="btn btn-danger"})
            @Html.ActionLink("Load","Load",new{id=@item.ArticuloId},new{@class="btn btn-warning"})
          </td>
        </td>
      </tr>
    }
  </tbody>
</table>

@section scripts{
    <script src="https://cdn.datatables.net/1.11.3/js/jquery.dataTables.min.js"></script>

    <script>
      $(document).ready(function(){
        //alert("Read load");
        var tabla = $('#tablelist').DataTable();
      });
    </script>
}
