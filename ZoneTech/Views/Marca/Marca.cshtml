@{

}
@model ZoneTech.Models.MarcaML
@{
    
}
<a class="btn btn-success" asp-controller="Marca" asp-action="CrearMarca">Agregar</a>

<h1>Lista de Marcas</h1>
<table class="table table-dark table-striped" id="tablelist">
  <thead>
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Marca</th>
      <th scope="col">Estado</th>
      <th>
        <th scope="col">Opción</th>
      </th>
    </tr>
  </thead>
  <tbody>
    @foreach (var item in ViewBag.list)
    {
      <tr>
        <td>@item.MarcaId</td>
        <td>@item.Nombre</td>
        <td>@item.EstadoId</td>
         <td>
          <td>
            @Html.ActionLink("Eliminar","EliminarMarca",new{id=@item.MarcaId},new{@class="btn btn-danger"})
            @Html.ActionLink("Editar","EditarMarca",new{id=@item.MarcaId},new{@class="btn btn-warning"})
          </td>
        </td>
      </tr>
    }
  </tbody>
</table>

@section scripts{
    <script src="https://cdn.datatables.net/1.11.3/js/jquery.dataTables.min.js"></script>

    <script>
      $(document).ready(function(){
        //alert("Read load");
        var tabla = $('#tablelist').DataTable();
      });
    </script>
}
